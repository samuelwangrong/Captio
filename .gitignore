node_modules
.next
.plasmo
build
dist
*.env
.env.local
.env.development.local
.env.test.local
.env.production.local
.DS_Store
*.log
*.tsbuildinfo
test-results/
playwright-report/

# This is a pnpm workspace — npm/yarn lockfiles are accidental.
package-lock.json
yarn.lock

# git push from this environment is rejected for workflow-file changes
# (OAuth token lacks the `workflow` scope) — keep it on disk, untracked,
# until that's fixed. See commit history for details.
.github/workflows/
